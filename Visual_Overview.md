# Screenshots of the User Interface

### 1. Dashboard – Initial State (Server Offline):
<img width="940" height="640" alt="image" src="https://github.com/user-attachments/assets/0da5a47f-07bd-49fe-aee5-f7bc9cd8a3dc" />

- __Description:__
This screenshot shows the application immediately after launch.
- __Visible Details:__
    - Application title: DoS Simulator
    - Server control buttons available
    - Server status shown as OFFLINE
    - Attack counters (200, 429, 403) initialized to zero
    - CPU and memory usage displayed at baseline
    - No live logs generated
- __Purpose:__
Confirms correct initialization and idle state before starting the server.

### 2. Event Logs Tab – Empty Database View:
<img width="940" height="438" alt="image" src="https://github.com/user-attachments/assets/33c045b8-2376-4d58-8916-9b01a8a02975" />

- __Description:__
This view displays the Event Logs tab when no database records are present.
- __Visible Details:__
    - Columns: Time, IP, Path, Status, Note
    - Filter option for status code
    - Export CSV and Clear DB Logs buttons
    - Empty table (no records yet)
- __Purpose:__
Confirms that logs are stored only after server activity begins.

### 3. Wireshark – Loopback Interface Selection:
<img width="940" height="289" alt="image" src="https://github.com/user-attachments/assets/ad28a6ce-cba9-42ab-b86c-4f02a5bd74f0" />

- __Description:__
Wireshark is configured to capture local traffic.
- __Visible Details:__
    - Adapter for loopback traffic capture selected
    - No external interfaces used
    - Ready to capture localhost traffic
- __Purpose:__
Ensures HTTP traffic generated by the simulator can be observed at the network layer.

### 4. Server Online & DoS Attack Simulation in Progress:
<img width="940" height="642" alt="image" src="https://github.com/user-attachments/assets/67188e55-cb09-4694-99c9-f3183b42157f" />

- __Description:__
This screenshot shows the application after the server is started and while a DoS attack simulation is actively running.
- __Visible Details:__
    - Server status changes to ONLINE
    - Open Browser button becomes available
    - Multiple simulated IP addresses generating concurrent requests
    - Live request logs updating continuously
    - Real-time increase in HTTP status counters
    - Combination of 200 (Allowed), 429 (Rate Limited), and 403 (Blocked) responses
    - CPU usage fluctuating due to active traffic processing
    - Memory usage remains stable throughout the simulation
- __Purpose:__
Confirms successful server startup, readiness to handle requests, and effective enforcement of rate limiting and blocking mechanisms during concurrent attack traffic.

### 5. CPU & Memory Utilization Graph
- __Description:__
The embedded graph displays system resource usage during attack execution.
- __Visible Details:__
    -	CPU usage spikes corresponding to traffic bursts
    -	Memory usage remains relatively constant
    - Graph updates in real time
- __Purpose:__
Shows that the simulation impacts CPU while remaining safe and controlled.

### 6. Wireshark – TCP and HTTP Traffic Capture
<img width="940" height="483" alt="image" src="https://github.com/user-attachments/assets/fc3fec1c-7a1e-44d3-b9fe-5a9a0aa21e25" />

- __Description:__
Wireshark capturing traffic on port 5050.
- __Visible Details:__
    - TCP SYN, ACK packets
    - HTTP GET requests
    - Loopback source and destination (127.0.0.1)
    - Port 5050 clearly visible
- __Purpose:__
Confirms that traffic generated by the simulator is real HTTP traffic.

### 7. Wireshark Filter – HTTP 200 Responses
<img width="940" height="596" alt="image" src="https://github.com/user-attachments/assets/dd2aaedb-43b6-4797-adcb-80d5ac45888b" />

- __Filter Used:__
`http.response.code == 200`
- __Visible Details:__
    - HTTP/1.1 200 OK responses
    - JSON payload returned
    - Requests accepted before rate limit is exceeded
- __Purpose:__
Validates allowed requests at the network level.

### 8. Wireshark Filter – HTTP 429 Responses
<img width="940" height="293" alt="image" src="https://github.com/user-attachments/assets/40db698c-12a1-451d-9f6c-a7ef3ae14ce4" />

- __Filter Used:__
`http.response.code == 429`
- __Visible Details:__
    - HTTP/1.1 429 Too Many Requests responses
    - Rate limiting applied before blocking
    - Matches GUI counters
- __Purpose:__
Validates rate-limiting behavior at both application and network levels.

### 9. Wireshark Filter – HTTP 403 Responses
<img width="940" height="559" alt="image" src="https://github.com/user-attachments/assets/2c6dfbe1-377d-4e97-b73b-ca289bad86be" />

- __Filter Used:__
`http.response.code == 403`
- __Visible Details:__
    - HTTP/1.1 403 Forbidden responses
    - JSON error messages
    - Repeated responses for same IPs
- __Purpose:__
Confirms automatic blocking after blacklist activation.

### 10. Event Logs – Filtered by HTTP 200
<img width="940" height="395" alt="image" src="https://github.com/user-attachments/assets/79bac2ca-54fc-4689-8734-ceae0afb20e0" />

- __Description:__
Database logs filtered to show only successful requests.
- __Visible Details:__
    - Status code 200
    - Multiple IP addresses
    - Note: Allowed
- __Purpose:__
Confirms persistent logging of allowed traffic.

### 11. Event Logs – Filtered by HTTP 429
<img width="940" height="341" alt="image" src="https://github.com/user-attachments/assets/7be21550-3e8b-432d-bdb3-c0176f9537be" />

- __Description:__
Database logs filtered for rate-limited requests.
- __Visible Details:__
    - Status code 429
    - Note: Rate Limit
    - Timestamped entries
- __Purpose:__
Demonstrates database-level tracking of rate-limiting events.

### 12. Event Logs – Filtered by HTTP 403
<img width="940" height="603" alt="image" src="https://github.com/user-attachments/assets/8d2dc934-6bce-49f5-a169-09265dea21ab" />

- __Description:__
Database logs filtered for blocked requests after automatic blacklisting.
- __Visible Details:__
    - Status code 403
    - Note: Blocked / Auto Blacklist
    - Timestamped entries
- __Purpose:__
Demonstrates database-level tracking of blocked requests and confirms enforcement of the automatic IP blacklisting mechanism.
















